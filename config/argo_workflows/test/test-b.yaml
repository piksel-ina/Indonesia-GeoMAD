apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: test-b-60-tile-1-
  namespace: argo-workflows
spec:
  serviceAccountName: argo-workflows-executor
  workflowTemplateRef:
    name: stats-s2-geomad-annual-r8xlarge
  arguments:
    parameters:
      - name: tiles
        value: "207:208,-19:-18"
      - name: cloud-cover-max
        value: "60"
      - name: output-path
        value: "gm_s2"

---
apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: test-b-60-tile-2-
  namespace: argo-workflows
spec:
  serviceAccountName: argo-workflows-executor
  workflowTemplateRef:
    name: stats-s2-geomad-annual-r8xlarge
  arguments:
    parameters:
      - name: tiles
        value: "211:213,-2:0"
      - name: cloud-cover-max
        value: "60"
      - name: output-path
        value: "gm_s2"
---
apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: test-b-60-tile-3-
  namespace: argo-workflows
spec:
  serviceAccountName: argo-workflows-executor
  workflowTemplateRef:
    name: stats-s2-geomad-annual-r8xlarge
  arguments:
    parameters:
      - name: tiles
        value: "256:258,-8:-6"
      - name: cloud-cover-max
        value: "60"
      - name: output-path
        value: "gm_s2"
---

