apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: test-a-80-tile-1-
  namespace: argo-workflows
spec:
  serviceAccountName: argo-workflows-executor
  workflowTemplateRef:
    name: stats-s2-geomad-annual-r4xlarge
  arguments:
    parameters:
      - name: tiles
        value: "268:269,-16:-15"
      - name: cloud-cover-max
        value: "80"
      - name: output-path
        value: "gm_s2_cc80"
---
apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: test-a-80-tile-2-
  namespace: argo-workflows
spec:
  serviceAccountName: argo-workflows-executor
  workflowTemplateRef:
    name: stats-s2-geomad-annual-r4xlarge
  arguments:
    parameters:
      - name: tiles
        value: "190:191,8:9"
      - name: cloud-cover-max
        value: "80"
      - name: output-path
        value: "gm_s2_cc80"

---
apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: test-a-80-tile-3-
  namespace: argo-workflows
spec:
  serviceAccountName: argo-workflows-executor
  workflowTemplateRef:
    name: stats-s2-geomad-annual-r4xlarge
  arguments:
    parameters:
      - name: tiles
        value: "271:272,-13:-12"
      - name: cloud-cover-max
        value: "80"
      - name: output-path
        value: "gm_s2_cc80"

---
apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: test-a-80-tile-4-
  namespace: argo-workflows
spec:
  serviceAccountName: argo-workflows-executor
  workflowTemplateRef:
    name: stats-s2-geomad-annual-r4xlarge
  arguments:
    parameters:
      - name: tiles
        value: "195:196,-2:-1"
      - name: cloud-cover-max
        value: "80"
      - name: output-path
        value: "gm_s2_cc80"
